import { render, screen } from "@testing-library/react";
import { expect, test, vi } from "vitest";
import { DocumentStatisticsPage } from "./DocumentStatisticsPage";
test("loads document data with the exact calendar period", async () => { const period = { unit: "month" as const, anchorLocalDay: "2026-07-01" }; const getDocStats = vi.fn().mockResolvedValue({ documentId: "doc", activeMs: 0, sessionCount: 0, averageSessionMs: null, pageVisits: 0, uniquePages: 0, revisits: 0, coverage: 0, realReviews: 0, recallRate: null, againCount: 0, lapses: 0, buckets: [], timeBuckets: [] }); render(<DocumentStatisticsPage documentId="doc" period={period} getDocStats={getDocStats} />); expect(await screen.findByText("Coverage")).toBeInTheDocument(); expect(screen.queryByRole("button", { name: "Heatmap" })).not.toBeInTheDocument(); expect(screen.getByRole("button", { name: "Graph" })).toHaveAttribute("aria-pressed", "true"); expect(getDocStats).toHaveBeenCalledWith("doc", period); });
